@objects
    form          #addtag
        name            #tag-name
        slug            #tag-slug
        parent          #parent
        description     #tag-description

= Form =
    form.slug:
        below form.name